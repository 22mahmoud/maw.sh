---
layout: 'base.njk'
eleventyNavigation:
  key: Home
  order: 1
---

<div class="hero-section">
  <div class="intro">
    <p class="text-xl" class="intro__subtitle">
      HEY 👋, MY NAME IS MAHMOUD ASHRAF.
    </p>
    {% Image "src/assets/images/author.jpeg", "Me talking a selfi" %}
    <h2 class="intro__title">
      A Software Engineer Who Builds stuff on the internet.
    </h2>
  </div>

  <div class="recent-blogs">
    <p class="text-xl">LATEST BLOG POSTS.</p>
    {%- for post in collections.latestPosts -%}
    <article>
      <h1 class="text-base p-name">
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </h1>
      <time class="dt-published text-sm" datetime="{{ post.data.date | iso }}">
        {{ post.data.date | date }}
      </time>
      <p class="p-summary">{{ post.data.description }}</p>
    </article>
    {%- endfor -%}
    <a href="/blog" class="recent-blogs__seemore"> See all posts → </a>
  </div>

  <div class="social">
    <p class="text-xl">FIND ME</p>
    {%- for account in social -%}
    <a rel="me" href="{{ account.href }}">
      {{ account.icon | safe }}
      {{ account.name }}
    </a>
    {%- endfor -%}
  </div>
</div>
